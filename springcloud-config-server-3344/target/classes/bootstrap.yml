server:
  port: 3344 #设置端口
spring:
  application:
    name: config-server #设置名称
  cloud:
    config:
      server:
        git:
          uri: https://gitee.com/yan418/springcloud-config.git
          force-pull: true #设置强行pull拉取